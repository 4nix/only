package entity

type item struct {
	info interface{}
}

// func NewItem(iGID int64) *item {
// 	oGroup := repository.FindGroup(iGID)

// 	return &item{info: oGroup}
// }

// func (entity *item) getInfo() interface{} {
// 	return entity.info
// }
